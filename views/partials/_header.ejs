<!--partials/_header.ejs-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'AvenirCon' %></title>
    <!-- Link to to main stylesheet -->
    <link rel="stylesheet" href="/css/style.css"> <!-- the main stylesheet -->
    <link rel="stylesheet" href="/css/public.css"> <!-- the main stylesheet -->

    <!--  added common head elements  -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<!-- actually included in public.css sheet-->
</head>
<style>
            /* Specific styles for the landing page */
        body.landing-page {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f8f9fa; /* Light background */
        }

        .landing-header {
            background-color: #343a40; /* Dark navbar */
            color: white;
            padding: 0.5rem 1rem;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
        .landing-header .navbar-brand {
            font-size: 1.75rem;
            font-weight: bold;
        }
        .landing-header .nav-link {
            color: rgba(255,255,255,.75);
            margin-left: 1rem;
            transition: color 0.2s ease-in-out;
        }
        .landing-header .nav-link:hover, .landing-header .nav-link.active {
            color: white;
        }
        .landing-header .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
         .landing-header .btn-outline-light {
            margin-left: 0.5rem;
        }

        .hero-section {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('/images/construction-hero.jpg'); /* Add a hero image */
            background-size: cover;
            background-position: center;
            color: white;
            padding: 6rem 1rem;
            text-align: center;
            flex-grow: 1; /* Make hero take available space if content is short */
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .hero-section h1 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.7);
        }
        .hero-section p.lead {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }
        .hero-section .cta-button {
            font-size: 1.2rem;
            padding: 0.75rem 1.5rem;
        }

        .features-section {
            padding: 4rem 1rem;
            background-color: #ffffff;
        }
        .features-section .feature-icon {
            font-size: 2.5rem;
            color: #007bff;
            margin-bottom: 1rem;
        }
        .footer {
           align-items: center; 
        }

        .landing-footer {
            background-color: #343a40;
            color: rgba(255,255,255,.7);
            padding: 2rem 1rem;
            text-align: center;
        }
        .landing-footer a {
            color: rgba(255,255,255,.9);
            text-decoration: none;
        }
        .landing-footer a:hover {
            color: white;
        }
  
</style>
<body>
    <body class="landing-page">
    <header class="landing-header sticky-top">
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container">
                <a class="navbar-brand" href="/"><i class="fas fa-hard-hat me-2"></i>AvenirCon</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#landingNavbar" aria-controls="landingNavbar" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="landingNavbar">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                         <li class="nav-item"><a href="/">Home</a></li>
                                     <!-- Added navigation links as needed -->
                        <li class="nav-item"><a class="nav-link" href="#features">Features</a></li>
                        <li class="nav-item"><a class="nav-link" href="#about">About</a></li>
                        <li class="nav-item"><a class="nav-link" href="#contact">Contact</a></li>
                        <% if (currentUser) { %>
                            <li class="nav-item">
                                <a href="/dashboard" class="btn btn-primary ms-lg-3">Go to Dashboard</a>
                               <!--  <a href="/logout">Logout (<%= currentUser.username %>)</a>-->
                                                     <a href="/logout" class="btn btn-danger">Logout</a>

                            </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a href="/login" class="nav-link">Login</a>
                            </li>
                            <li class="nav-item">
                                <a href="/register" class="btn btn-outline-light ms-lg-2">Register</a>
                            </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
    <hr>
        <!-- Flash Messages Display -->
        <% if (success_msg && success_msg.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success_msg %>
            </div>
        <% } %>
        <% if (error_msg && error_msg.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error_msg %>
            </div>
        <% } %>
        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        
        

    </header>
    
    <main>
    