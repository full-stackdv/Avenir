

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0"><%= pageTitle %></h2>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <form action="/admin/post-categories/<%= category.id %>/edit" method="POST">
                <%- include('_form', { category: category, errors: (typeof errors !== 'undefined' ? errors : []) }) %>
            </form>
        </div>
    </div>
</div>
